import React from 'react'

const HeadTitle = ({ TitleText }) => {
    return (
            <div className="d-flex justify-content-center mb-5 color-white fs-1">{TitleText}</div>
    )
}

export default HeadTitle
